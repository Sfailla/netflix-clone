import React from 'react';
import { Header } from '../containers';

export default function SelectProfileContainer({ user, setProfile }) {
	return (
		<React.Fragment>
			<Header nav={true} bg={false} />
		</React.Fragment>
	);
}
